# Redirect rules for Single Page Application (SPA) routing
# This ensures that page refreshes work correctly on any route

# Static assets should be served normally (don't redirect)
/static/* /static/:splat 200
/favicon.ico /favicon.ico 200
/manifest.json /manifest.json 200
/logo192.png /logo192.png 200
/logo512.png /logo512.png 200

# API routes (if any) - preserve them
/api/* /api/:splat 200

# All other routes should serve index.html for client-side routing
# This handles page refreshes on routes like /app/job-description
/* /index.html 200
